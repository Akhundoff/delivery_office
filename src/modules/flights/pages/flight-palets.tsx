import { FC } from 'react';
import { PageContent } from '@shared/styled/page-content';
import { FlightPaletsContainer } from '../containers';

export const FlightPaletsPage: FC = () => (
  <PageContent>
    <FlightPaletsContainer />
  </PageContent>
);
